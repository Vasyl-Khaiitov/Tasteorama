export const selectNameFilter = (state) => state.filter.name;
export const selectCategoryFilter = (state) => state.filter.category;
export const selectIngredientFilter = (state) => state.filter.ingredient;